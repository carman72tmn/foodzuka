# Лог обслуживания проекта FoodTech
Дата: 2026-04-23
Время: 00:24
Сессия: 1
Задача: Очистка кэша, удаление ненужных файлов и пересборка проекта.

## Выполненные действия:

1.  **Очистка файловой системы на VPS**:
    - Создана директория `/root/foodzuka/old_scripts/`.
    - Перемещены все временные файлы (`.py`, `.sql`, `.hex`, `.tmp`, `.txt`, `.ini`) и архивы (`.zip`, `.tar.gz`) из корня проекта в `old_scripts/`.
    - Удалены все директории `__pycache__` в проекте.
2.  **Очистка Docker**:
    - Выполнена очистка неиспользуемых (dangling) образов. Освобождено **1.647GB** места на диске.
3.  **Пересборка проекта**:
    - Проект остановлен (`docker compose down`).
    - Запущена полная пересборка всех сервисов без использования кэша (`docker compose build --no-cache`).
    - Сервисы запущены в фоновом режиме (`docker compose up -d`).
4.  **Обновление документации**:
    - В `system_faq.md` добавлен раздел "Обслуживание и очистка (Maintenance)" с описанием регламента работ.
    - В `sitenav.md` зафиксирован статус обновлений.

## Результаты:
- Оптимизировано место на сервере.
- Структура корня проекта очищена от "мусорных" файлов.
- Система перезапущена с актуальными сборками всех компонентов.
