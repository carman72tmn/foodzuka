server {
    listen 80;
    server_name _;

    # ACME Challenge for Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all HTTP traffic to HTTPS (ЗАКОММЕНТИРОВАТЬ ПРИ ПЕРВОМ ЗАПУСКЕ, ЕСЛИ НУЖНО)
    location / {
        return 301 https://$host$request_uri;
    }
}

# ПОСЛЕ ПОЛУЧЕНИЯ СЕРТИФИКАТА РАСКОММЕНТИРУЙТЕ ЭТОТ БЛОК
# server {
#     listen 443 ssl;
#     server_name _;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#
#     # Backend API
#     location /api {
#         proxy_pass http://foodtech-backend:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # Admin Panel
#     location /admin {
#         proxy_pass http://foodtech-admin:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # Frontend
#     location / {
#         proxy_pass http://foodtech-frontend:5173;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
