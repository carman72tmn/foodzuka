# Log Session: UI Refinement and Item Persistence
Date: 2026-05-04
Time: 21:13
Task: Редизайн карточки заказа, перенос блоков, исправление логики удаления товаров и суммирования скидок.

## Выполненные действия:

1. **Frontend (OrderDetailModal.vue)**:
   - Перенесен блок "КОММЕНТАРИЙ" под блок "КЛИЕНТ".
   - Перенесен блок "ИНФОРМАЦИЯ О ЗАКАЗЕ" в самый низ карточки.
   - Удален блок "Создан в iiko" из метаданных шапки.
   - Улучшена читаемость: темно-серый текст вместо черного, пастельные тона для элементов.
   - Исправлен цвет текста в детализации платежей (устранен эффект "сливания" с фоном).
   - Внедрена визуализация удаленных товаров (зачеркивание + пастельно-красный фон) на основе флага `deleted`.
   - Обновлен блок "ИТОГО К ОПЛАТЕ": тонкая пунктирная линия, акцент на шрифтах.

2. **Backend (iiko_sync_service.py)**:
   - **Soft Delete**: Теперь позиции, удаленные в iiko, не пропадают из БД, а помечаются `deleted: true`. Это сохраняет историю изменений для персонала.
   - **Discount Fix**: 
     - Исправлено двойное суммирование бонусов и скидок. `total_discount` теперь содержит только чистые скидки (вычитаются бонусы, если iiko передала их как скидки).
     - Добавлена дедупликация скидок по ID/имени во избежание роста суммы при повторных вебхуках.
   - **Calculated Total**: Подытог теперь рассчитывается только по активным (не удаленным) позициям.

3. **Infrastructure**:
   - Обновлен `order-detail-soft.css` (новые классы `.border-t-dotted`, `.deleted-item-row`).
   - Синхронизация с VPS выполнена (Build, Restart).

4. **Documentation**:
   - Обновлен `system_faq.md` (раздел 51).
   - Обновлен `sql_faq.md` (структура `order_items_details`).

## Статус:
Все изменения протестированы на локальной среде и выгружены на VPS.
Логин в админку: 0001 / 121212.
URL: vezuroll.ru/admin
