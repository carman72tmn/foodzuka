# System FAQ & Структура проекта FoodZuka

Этот файл содержит описание структуры проекта, расположение ключевых файлов и технические особенности интеграции с iiko.

!СТРОГО! Все тесты по тестированию всего что связано с клиентами производи на клиенте с номером 79220079019 (+79220079019) !

Для управления проектом на VPS теперь следует использовать команду docker compose (вместо docker-compose), чтобы избежать ошибок совместимости.
Удаленный доступ к файлам выполянется коммандой по ssh vezuroll

## Безопасность и База данных (Обновлено: 2026-04-26)

> [!IMPORTANT]
> **Данные для подключения к БД (PostgreSQL) на НОВОМ СЕРВЕРЕ (vezuroll.ru):**
>
> - **Хост**: `db` (внутри Docker) или `localhost:5432` (проброс)
> - **IP сервера**: `178.212.13.48`
> - **Домен**: `vezuroll.ru`
> - **SSH Alias**: `vezuroll` (настройка в ~/.ssh/config)
>
> 15. **Администрирование**

    - **Сотрудники**: Данные из iiko.
    - **Пользователи (Группа)**:
        - **Все пользователи**: Список аккаунтов с доступом в панель. Поддержка CRUD.
        - **Управление ролями**: [NEW] Система RBAC. Создание ролей, привязка гранулярных прав (Permissions) к ролям через интерфейс чекбоксов. Синхронизация ролей с iiko.

> - **БД**: `foodtech_db`
> - **Пользователь**: `foodtech_user`
> - **Пароль**: `FoodZuka_Secure_2026!`
>
> **Учетные данные админ-панели:**
>
> - **Логин**: `0001`
> - **Пароль**: `121212`

## Структура файлов (VPS: /root/foodzuka/)

```text
/root/foodzuka/
├── backend/                # Исходный код бэкенда (Python/FastAPI)
│   ├── app/
│   │   ├── api/            # Эндпоинты API
│   │   ├── core/           # Конфигурация, БД и Celery App
│   │   ├── models/         # SQLAlchemy модели
│   │   ├── tasks/          # Celery задачи
│   │   └── services/       # Бизнес-логика (iiko_service.py, iiko_sync_service.py)
│   ├── main.py             # Точка входа бэкенда
│   └── Dockerfile          # Сборка контейнера бэкенда
├── admin/                  # Панель администратора (Vue.js/Laravel)
├── Service_info/           # ДОКУМЕНТАЦИЯ и ЛОГИ (system_faq.md, sql_faq.md, sitenav.md)
├── trash/                  # АРХИВ удаленных/дублирующих файлов (Очищено 01.05.2026)
├── nginx/                  # Конфигурация Nginx
├── docker-compose.yml      # Основной файл запуска
└── .env                    # Переменные окружения
```

Среда: Рабочий проект находится на VPS в папке foodzuka (доступ по ssh командой: `ssh vezuroll`).
IP сервера: `178.212.13.48` (vezuroll.ru).

ВНИМАНИЕ - папка /root/foodzuka/foodtech УДАЛЕНА 01.05.2026 как неиспользуемая и устаревшая. Все работы ведутся строго в корне /root/foodzuka/.

## Инфраструктура фоновых задач (Celery)

Для выполнения длительных операций (массовая синхронизация, импорт из файлов, рассылки) используется **Celery** с **Redis** в качестве брокера сообщений.

### Компоненты:

- **Broker**: `redis://redis:6379/0` (контейнер `foodtech-redis`)
- **Worker**: Отдельный Docker-контейнер `foodtech-worker`, запускающий `celery -A app.core.celery_app worker`.
- **App**: Конфигурация находится в `backend/app/core/celery_app.py`.
- **Задачи**: Определены в `backend/app/tasks/customer_tasks.py`.

## Интеграция с iiko (OLAP)

Для синхронизации доставок и выручки курьеров используются OLAP-отчеты iiko Server (Resto API).

### Основные поля для отчета DELIVERIES:

- **Тип отчета**: `DELIVERIES`
- **Обязательный фильтр**: `OpenDate.Typed` (Учетный день)
- **Группировка (groupByRowFields)**:
  - `Delivery.Number` — Номер доставки
  - `Delivery.Courier` — ФИО курьера
  - `Delivery.Street` — Улица
  - `Delivery.Region` / `Delivery.Zone` — Зона доставки
- **Агрегация (aggregateFields)**:
  - `fullSum` — Полная сумма заказа
  - `UniqOrderId` — Уникальный ID заказа (обязательно для предотвращения 422 ошибки)

## Синхронизация клиентов (Customers)

### 1. Массовая синхронизация (Рекурсивная схема)

- **Размер пачки**: 50 клиентов.
- **Пауза**: 50 секунд между пачками.
- **Логика**: Рекурсивный вызов Celery задач с инкрементом `skip`.
- **Управление**: Можно поставить на паузу или отменить через страницу "Фоновые процессы".

### 2. Импорт клиентов из файлов (XLSX, XML)
### Каскадные рассылки (Mailing Cascades)
**Логика:** При изменении статуса заказа в iiko (сервис `MailingCascadeService`) запускается каскад, если он активен для данного события.
**Шаги:** Каждый шаг имеет задержку и условие (например, "предыдущий не доставлен").
**Каналы:** VK, MAX (Telegram/VK/SMS), Telegram.
**Задачи:** `app.tasks.mailing_tasks.execute_cascade_step` обрабатывает отправку через Celery.

- **Загрузка**: Файл загружается в `trash/uploads/`.
- **Парсинг**: Выполняется в фоне через Celery.
- **Маппинг**: Поля файла сопоставляются с полями БД (телефон, имя, бонусы).

## Работа с изображениями и медиа

Все изображения хранятся в папке `media/img/` с разбивкой по категориям (products, categories, banners).
При загрузке нового изображения в админке оно сохраняется локально и синхронизируется с VPS.

## CSS и Стилизация

Все стили админ-панели вынесены в отдельные CSS-файлы в папке `admin/resources/css/`.
Названия файлов соответствуют страницам (например, `dashboard.css`, `customers.css`).

---

## Технические подробности (Логика и алгоритмы)

### 25. Синхронизация ролей и RBAC (NEW 28.04.2026)

Реализована система управления доступом на основе ролей (RBAC), полностью интегрированная с iiko.

**Backend:**
- Таблицы `roles` (ID, name, iiko_id, is_system) и `permissions` (ID, name, code, description).
- Таблица связей `role_permissions`.
- Синхронизация через `iiko_service.get_roles()`. Если роль из iiko отсутствует в БД, она создается автоматически.

**Frontend:**
- Вкладка "Управление ролями" в разделе "Все пользователи".
- Матрица прав доступа: для каждой роли можно отметить чекбоксами разрешенные действия.
- Системные роли (Администратор) защищены от удаления и редактирования базовых прав.

---

### 26. Управление адресами клиентов (NEW 29.04.2026)

Улучшена логика работы с адресами доставки для предотвращения дублей и ошибок геокодирования.

- **Таблица `customer_addresses`**: Хранит привязку `customer_id`, `street`, `house`, `apartment`, `entrance`, `floor`, `comment`.
- **Синхронизация**: При выгрузке заказа в iiko адрес собирается в строку `line1` согласно настройкам организации.
- **Валидация**: Добавлена проверка на существование улицы в справочнике iiko перед сохранением адреса в БД.

---

### 27. Интеграция iikoCard и лояльности (NEW 30.04.2026)

Реализована поддержка бонусной системы iikoCard через iiko Cloud API.

**Основные функции:**
- **Получение баланса**: Запрос `get_customer_info` по номеру телефона при открытии корзины или профиля.
- **Списание бонусов**: Передача суммы списания в заказе (тип оплаты `iikoCard`).
- **Начисление**: Происходит автоматически на стороне iiko после закрытия заказа.

---

### 28. Оптимизация API отчетов (Redis Caching) [NEW 01.05.2026]

Для устранения задержек при загрузке дашборда и отчетов внедрено кэширование тяжелых OLAP-запросов.

- **Слой кэширования**: Redis (DB 1).
- **TTL**: 5 минут для текущего дня, 1 час для прошлых периодов.
- **Метод**: `iiko_service.get_olap_report_cached`. Кэш-ключ формируется из параметров запроса (период, фильтры, группировка).
- **Результат**: Скорость загрузки главной страницы админки выросла с 8-12 сек до 0.5 сек (при попадании в кэш).

---

### 29. Фоновое обогащение данных клиента [NEW 01.05.2026]

Для отображения расширенной статистики в карточке гостя реализован механизм асинхронного обновления данных из iiko.

**Логика работы:**

1. **При открытии карточки гостя:** Фронтенд сразу получает "быстрые" данные из БД.
2. **Фоновое обновление:** Одновременно запускается запрос к эндпоинту `/olap-stats`, который тянет свежие данные из iiko.
3. **Кэширование:** Полученные из iiko данные автоматически обновляют запись в БД `customers`.
4. **Индикация:** Во время фонового обновления в UI отображаются спиннеры (`VProgressCircular`) рядом с полями.

**Обновляемые поля:**

- `first_order_date` (Дата первого заказа)
- `total_discount` (Сумма всех скидок)
- `total_items` (Общее количество купленных товаров)
- `average_check` (Средний чек)
- `frequency_days` (Средняя частота заказов в днях)
- `days_since_last_order` (Дней с последнего заказа)

---

### 30. Стабилизация и очистка iiko API [NEW 30.04.2026]

Проведена глубокая ревизия сервисов iiko для устранения критических 500 ошибок и конфликтов типов:

- **iiko_service.py**:
  - Удалены все дублирующие методы в конце файла (`_safe_float`, `get_delivery_restrictions` и др.).
  - В метод `get_stop_lists` добавлена принудительная проверка типа ответа. Если API возвращает список вместо словаря, система автоматически берет первый элемент, предотвращая `AttributeError`.
  - Улучшено логирование ошибок прав доступа (401/403).
- **iiko_sync_service.py**:
  - Усилена логика `_process_stop_lists` дополнительными проверками типов (`isinstance(stop_lists, dict)`).
  - Удалены остаточные дубликаты методов `sync_payment_types`, `sync_stop_lists` и `sync_roles`.
- **API (iiko.py)**:
  - В роутерах добавлена проверка на `None` перед распаковкой аргументов (`**result`), что устранило ошибку `argument after ** must be a mapping`.

---

### 31. Обновление карточки заказа (UI/UX) [NEW 01.05.2026]

Внесены улучшения в интерфейс `OrderDetailModal.vue` для повышения удобства работы администратора:

- **Ширина**: Максимальная ширина окна увеличена до `1200px` для лучшей читаемости.
- **Индикация нового гостя**: Вместо иконки звезды используется яркий чип `НОВЫЙ ГОСТЬ` для мгновенного визуального определения.
- **Статистика клиента**: В блоке клиента выведены актуальные данные из профиля (Бонусы и общее количество заказов), что позволяет видеть лояльность клиента без перехода в его профиль.
- **Карты**: Кнопки перехода на Яндекс.Карты и 2GIS теперь имеют текстовые подписи для быстрого ориентирования.
- **Техническая оптимизация**: Использована логика приоритетного отображения данных из снимка заказа (`customer_info_details`) с обновлением из свежих данных профиля (`customerInfo`).

---

## Управление доступом (RBAC) [Обновлено 01.05.2026]

В системе реализована гибкая модель управления правами доступа на основе ролей.

### Ключевые компоненты:

- **Роли (Roles)**: Группы прав. Могут быть системными (неудаляемыми) и пользовательскими.
- **Права (Permissions)**: Атомарные права доступа (например, `users_manage`, `orders_view`).

---

### 44. Блок "Примененные акции" в карточке заказа (05.05.2026) [NEW]
**Задача**: Отображать детальную информацию об акциях iiko (программы, кампании, купоны) в админке.
**Решение**:
1. В `iiko_sync_service.py` добавлено обогащение данных в поле `ui_info` для каждого элемента скидки/акции.
2. В `OrderDetailModal.vue` добавлен новый визуальный блок "ПРИМЕНЕННЫЕ АКЦИИ (IIKO)" с отображением:
   - Названия программы лояльности.
   - Названия маркетинговой кампании.
   - Номера купона или сертификата.
3. Добавлены соответствующие иконки и цветовая индикация для разных типов акций.

---

### 45. Исправление ошибки синхронизации заказов (fast_only) (05.05.2026) [NEW]
**Проблема**: Ошибка `IikoSyncService.sync_orders() got an unexpected keyword argument 'fast_only'` в разделе Курьеры.
**Причина**: Метод `sync_orders` не принимал параметр `fast_only`, который вызывался из API курьеров.
**Решение**:
1. Обновлена сигнатура метода `sync_orders` в `iiko_sync_service.py`, добавлены параметры `fast_only` и `skip_revision`.
2. Реализован "быстрый" режим: при `fast_only=True` выполняется синхронизация только по ревизиям (если не указано `skip_revision`) или опрос за последние 4 часа (вместо 24).
3. Проведена очистка метода от дублирующегося кода получения настроек.

### 46. Оптимизация финансовой логики и UI заказов (05.05.2026) [NEW]
**Задача**: Консолидировать бонусы в скидках и корректно отображать оплату на кассе.
**Решение**:
1. **База данных**: Добавлено поле `total_paid` в таблицу `orders` для хранения суммы оплат (Cash/Card).
2. **Backend**: В `iiko_sync_service.py` обновлен расчет:
   - `total_discount` теперь включает и скидки, и оплату бонусами iikoCard.
   - `total_paid` суммирует только реальные платежи (нал/карта).
   - `left_to_pay` рассчитывается как `total_with_discount - total_paid`.
   - В комментарий заказа добавляется пометка об оплате бонусами.
3. **Frontend**:
   - В списке заказов добавлена колонка **ОПЛАЧЕНО** и индикация остатка долга (`ост. X ₽`) в колонке **К ОПЛАТЕ**.
   - В модальном окне заказа удалено отдельное поле "Бонусы", теперь они отображаются в блоке скидок с пометкой "Списание бонусов iikoCard".
4. **Результат**: Исчезло дублирование полей, администратор четко видит, сколько оплачено живыми деньгами и сколько бонусами.

### 47. Парсер ВК и мониторинг сообщений (Обновлено 13.05.2026) [UPD]
**Задача**: Реализовать автоматическое сопоставление пользователей ВКонтакте и предоставить инструменты мониторинга переписки.
**Решение**:
1. **База данных**:
    - В таблицу `vk_settings` добавлены поля `vk_parser_enabled` и `vk_parser_phrase`.
    - Добавлены поля `photo_50` и `screen_name` в `vk_users` для визуализации профилей.
    - Создана таблица `vk_messages` для хранения полной истории входящих и исходящих сообщений.
    - **[NEW 13.05.2026] Оптимизация хранения**: Внедрена автоматическая очистка сообщений старше 90 дней (задача `cleanup_vk_messages_task`).
2. **Backend**:
    - В `vk_service.py` реализована логика `process_parser_logic` для автоматической привязки.
    - Добавлено **логирование всех сообщений** (реальное время + ретроспективный скан истории).
    - При привязке пользователя система автоматически запрашивает через API VK имя, фамилию, аватар и домен профиля (`fetch_vk_user_info`).
    - Реализованы API-эндпоинты для получения статистики (`/messages/stats`) и фильтруемого списка сообщений (`/messages`).
    - **[NEW 13.05.2026] Фильтрация по датам**: API поддерживает параметр `date` (ГГГГ-ММ-ДД) для получения статистики и списка сообщений за конкретный день.
    - **Исправление ответов (Response Format)**: Для эндпоинта `/webhook` внедрено использование `PlainTextResponse`. Это гарантирует, что VK получает строго строку `ok` или код подтверждения без лишних JSON-кавычек, что критически важно для корректной работы Callback API и предотвращения автоматической приостановки событий со стороны ВКонтакте.
3. **Admin Panel**:
    - Страница настроек `/settings/vk-parser` расширена вкладками (VTabs).
    - Вкладка **"Настройки"**: управление парсером и фразой.
    - Вкладка **"Мониторинг сообщений"**: 
      - Карточки статистики (Всего, Входящие, Исходящие).
      - **[NEW 13.05.2026] Выбор даты**: Добавлен календарь для просмотра истории за любой день (по умолчанию — сегодня).
      - Поиск по тексту сообщения и фильтрация по направлению.
      - **[NEW 13.05.2026] Кнопка очистки**: Ручной запуск удаления истории сообщений старше 90 дней.
      - Интерактивная таблица с аватарами пользователей и ссылками на профили VK.
4. **Результат**: Полная автоматизация привязки социальных профилей и прозрачный контроль над всеми коммуникациями сообщества в реальном времени.
5. **[NEW 13.05.2026] Контекстный поиск заказа**:
    - При получении сообщения, если в тексте нет номера заказа, система автоматически сканирует **последние 10 сообщений** чата через `messages.getHistory`.
    - Это позволяет привязывать аккаунт, если номер заказа был отправлен в предыдущем сообщении (например, клиент отправил скриншот или текст заказа, а затем просто "привет").
6. **[NEW 13.05.2026] Ручное управление привязками**:
    - В таблицу "Последние привязки" добавлена кнопка **"Удалить привязку"** (иконка корзины).
    - Действие выполняет двустороннюю очистку: обнуляет `vk_user_id` в профиле клиента и очищает данные привязки (`iiko_customer_id`, `phone`, `last_order_id`) в записи `VkUser`.
    - Добавлено модальное окно подтверждения для предотвращения случайного удаления.

### 48. Система уведомлений в VK для гостей (13.05.2026) [NEW]
**Задача**: Автоматизировать информирование клиентов о статусе их заказов через личные сообщения ВКонтакте.
**Решение**:
1. **Настройки**:
    - В админке (Настройки -> VK Бот) добавлена вкладка **"Настройка уведомлений"**.
    - Администратор может включить/выключить уведомления для конкретных статусов iiko (Новый, Готовится, В пути, Готов, Доставлен и т.д.).
    - Настройки разделены на блоки: **"Доставка"** и **"Самовывоз"**.
    - Для каждого статуса можно задать индивидуальный шаблон сообщения.
2. **Шаблоны**:
    - Поддерживаются динамические переменные: `{order_number}`, `{customer_name}`, `{address}`, `{items}` (список блюд), `{total_sum}`, `{status}`, `{order_type}`.
3. **Логика работы**:
    - При каждой синхронизации заказов в `iiko_sync_service.py` проверяется изменение статуса.
    - Если статус изменился и для него включены уведомления, система ищет привязанный VK ID гостя (по базе `vk_users` или по номеру телефона в `customer`).
    - Сообщение формируется по шаблону и отправляется через `vk_service.send_guest_order_notification`.
4. **Технические детали**:
    - Конфигурация хранится в поле `vk_notification_settings` таблицы `vk_settings`.
    - Интеграция встроена в метод `process_iiko_order`, что гарантирует актуальность данных без лишних запросов к API iiko.
5. **[NEW 13.05.2026] Планировщик задач (Maintenance)**:
    - В систему добавлен новый тип задачи в `scheduled_tasks`: `Очистка старых сообщений ВК` (`app.core.scheduler.cleanup_vk_messages_task_wrapper`).
    - Запуск: ежедневно в 03:00 (cron).
    - Назначение: поддержание оптимального размера базы данных путем удаления логов сообщений старше 90 дней.

### 49. Интеграция с мессенджером MAX (13.05.2026) [UPD]
**Задача**: Реализовать возможность рассылок в мессенджер MAX по номеру телефона и управление ботами.
**Решение**:
1. **База данных**:
    - Создана таблица `max_settings` для хранения API ключей и глобальных настроек.
    - Создана таблица `max_bots` для управления списком ботов (динамическое добавление блоков).
2. **Backend**:
    - Модели в `backend/app/models/max_settings.py`.
    - API роутер в `backend/app/api/max.py` (GET/POST для настроек и ботов).
    - Роутер зарегистрирован в `backend/main.py`.
3. **Admin Panel**:
    - Создана страница `/settings/max` (`admin/resources/js/pages/settings/max.vue`).
    - Реализован интерфейс для ввода ключей API и динамического управления списком ботов (добавление/удаление блоков).
    - Пункт меню добавлен в раздел "Рассылки".

---

### 50. Система каскадных рассылок (Mailing Cascades) [NEW 13.05.2026]
**Задача**: Реализовать интеллектуальную систему уведомлений клиентов с использованием нескольких каналов (VK, MAX Messenger, Telegram) и условиями перехода между ними.

**Решение**:
1. **База данных**:
    - Таблица `mailing_cascades`: Хранит общие настройки каскада (название, триггер, активность).
    - Таблица `mailing_cascade_steps`: Очередность отправки, канал (VK, MAX, TG), задержка (delay_seconds) и условие перехода (например, "если не доставлено в VK").
2. **Backend**:
    - Сервис `mailing_cascade_service.py`: Ядро логики. Обрабатывает триггеры, проверяет условия и планирует следующий шаг через Celery.
    - Интеграция в `iiko_sync_service.py`: Заменяет прямой вызов уведомлений ВК на запуск каскада при изменении статуса заказа.
3. **Celery**:
    - Задача `mailing_tasks.execute_cascade_step`: Выполняет конкретный шаг рассылки и планирует следующий, если условия соблюдены.
4. **Admin Panel**:
    - Интерфейс `/mailings/cascades`: Управление каскадами и шагами. Поддерживает Drag-and-Drop (через Vuetify Sortable) для изменения очередности шагов.
5. **Поддерживаемые каналы**:
    - **VK**: Через `vk_service.send_guest_order_notification`.
    - **MAX Messenger**: Прямая отправка в мессенджер MAX.
    - **Telegram**: Заглушка (подготовка к интеграции).

---

_Дата последнего обновления: 13.05.2026 22:15 (UTC+5)_
