# Руководство по интеграции с iiko Cloud API (Transport)

Данный документ содержит выжимку информации, необходимой для интеграции проекта Foodtech с системой iiko.

## 1. Основные концепции

iiko Cloud API (ранее iiko Transport) — это современный API для работы с досавкой, меню и лояльностью.

- **Base URL**: `https://api-ru.iiko.services/api/1/`
- **Формат данных**: JSON
- **Авторизация**: Bearer токен (действует 15 минут)

## 2. Настройка API (Алгоритм подключения)

Для начала работы необходимо получить **API Login** в личном кабинете iikoWeb.

1.  Зайдите в **iikoWeb** под правами администратора.
2.  Перейдите в раздел **Настройки** -> **Внешние API**.
3.  Создайте новый API-логин (запишите его, это секретный ключ).
4.  Убедитесь, что у логина есть права на работу с организациями и доставкой.
5.  В iiko front (на кассах) должен быть активен модуль **iikoCloud**.

## 3. Ключевые методы API

### 🔑 Авторизация (`POST /access_token`)

Получение временного токена.

- **Request**: `{ "apiLogin": "ваш_логин" }`
- **Response**: Строка с токеном или JSON `{ "token": "..." }`.
- **Usage**: Заголовок `Authorization: Bearer <token>`.

### 🏢 Получение организаций (`POST /organizations`)

Список доступных ресторанов и их ID.

- **Important**: Почти все остальные методы требуют `organizationId`.

### 📋 Получение меню (`POST /menu`)

Синхронизация товаров и категорий.

- **Request**: `{ "organizationIds": ["uuid"] }`
- **Data**: Возвращает дерево групп (категорий) и список товаров с ценами, картинками и модификаторами.

### 🚴 Создание заказа (`POST /deliveries/create`)

Отправка заказа в ресторан.

- **Основные поля**:
  - `organizationId`: ID ресторана.
  - `terminalGroupId`: ID кассового терминала (получается через `GET /terminal_groups`).
  - `order`: Объект с деталями (клиент, телефон, адрес, состав заказа `items`).
  - `payments`: Тип оплаты (наличные, карта и т.д.).

### 🛑 Стоп-листы (`POST /stop_lists`)

Проверка доступности блюд в реальном времени.

## 4. Обработка ошибок

Основные коды ошибок:

- **400**: Ошибка в структуре запроса (невалидный JSON или отсутствуют поля).
- **401**: Токен истек (необходимо обновить через `/access_token`).
- **408**: Timeout (iiko front не ответил вовремя).
- **500**: Внутренняя ошибка iiko.

[Подробный справочник ошибок apimenu.ru](https://apimenu.ru/errors)

## 5. Полезные ссылки и ресурсы

### Официальная документация

- [iiko Cloud API (Redoc/Swagger)](https://api-ru.iiko.services/docs) — интерактивная документация.
- [iiko Help Center (Cloud API)](https://ru.iiko.help/articles/#!api-documentations/iikocloud-api) — общие статьи по настройке.
- [Integrations Guide](https://ru.iiko.help/articles/#!api-documentations/integraciya-storonnikh-prilojenij).

### Обучающие материалы и FAQ

- [ApiMenu FAQ](https://apimenu.ru/faq) — ответы на частые вопросы (авторизация, меню, заказы).
- [Инструкция по настройке в iikoWeb (LoyalHub)](https://docs.loyalhub.ru/instrukcii-po-vnedreniyu/nastroika-integracii-cloud-api-v-iikoweb) — пошагово с картинками.
- [Google Doc: iiko Cloud API Guide](https://docs.google.com/document/d/1kuhs94UV_0oUkI2CI3uOsNo_dydmh9Q0MFoDWmhzwxc/edit?tab=t.0#heading=h.1fze0wb8w29m).

### Разработчикам

- [GitHub: iiko-api (PHP Library)](https://github.com/fisher85/iiko-api) — примеры структур и логики.
- [Redocly](https://github.com/Redocly/redoc) — инструмент для чтения iiko-спецификаций.

## 6. Рекомендации по реализации

1.  **Кэширование токена**: Храните токен в Redis/Cache на 14 минут, чтобы не запрашивать его на каждый чих.
2.  **Периодическая синхронизация**: Синхронизируйте меню раз в 1-2 часа, а стоп-листы — каждые 5-10 минут.
3.  **Логирование**: С сохраняйте `correlationId` из ответов iiko — он нужен техподдержке iiko при разборе ошибок.
