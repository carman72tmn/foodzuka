# Log Gemini: 2026-04-26 05:40
## Задача: Реализация рекурсивной синхронизации гостей и настройка Worker на VPS

### Выполненные действия:

1.  **Backend (Локально и VPS)**:
    *   `iiko_service.py`: Добавлен метод `get_customers` для получения списка гостей через iiko Cloud API.
    *   `iiko_sync_service.py`: Добавлен метод `create_customer_from_order` для автоматического создания карточки гостя при поступлении заказа.
    *   `customer_tasks.py`: Реализована рекурсивная задача `sync_customers_batch` (50 чел / 50 сек).
    *   `api/customers.py`: Эндпоинт `/sync-all` переведен на запуск фоновой задачи Celery.
    *   `models/sync_log.py`: Добавлена модель `SyncStatus` для отслеживания прогресса.
    *   `core/celery_app.py`: Создан файл конфигурации Celery.

2.  **Infrastructure (VPS)**:
    *   `docker-compose.yml`: Добавлен сервис `worker`, исправлены пути к сертификатам Let's Encrypt (`./foodtech/certbot/conf`).
    *   `requirements.txt`: Добавлена зависимость `celery[redis]`.
    *   Выполнена пересборка контейнеров: `docker compose up -d --build --no-cache`.
    *   Исправлена критическая ошибка Nginx `host not found in upstream "backend"` путем форсированного перезапуска после стабилизации бэкенда.

3.  **Documentation**:
    *   Обновлен `sql_faq.md`: добавлены поля `notes`, `last_order_date` и таблица `sync_statuses`.
    *   Обновлен `system_faq.md`: добавлена архитектура Celery и логика рекурсивной синхронизации.
    *   Обновлен `sitenav.md`: описаны изменения в разделе Клиенты.

### Текущий статус:
*   Все контейнеры на VPS в статусе `Up`.
*   Worker успешно подключен к Redis и готов к работе.
*   Nginx настроен и проксирует запросы на бэкенд.
*   Все файлы сохранены в кодировке UTF-8.

### Следующие шаги:
*   Ожидание подтверждения от пользователя после тестирования кнопки синхронизации в админке.
*   Мониторинг логов воркера при первом запуске массовой синхронизации.
